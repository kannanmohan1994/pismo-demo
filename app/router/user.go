package router

import (
	"github.com/gin-gonic/gin"
)

func (c *Capsule) UserRoutes(r *gin.RouterGroup) {
	//userHandler := c.Handler.UserHandler

	// userV1 := r.Group("/users")
	// userV1.POST("/register", userHandler.HandleCreateAccount)
	// userV1.POST("/login", userHandler.HandleLoginUser)
}
